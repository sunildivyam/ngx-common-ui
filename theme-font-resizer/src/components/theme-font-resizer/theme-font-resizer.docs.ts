
import { THEME_FONT_RESIZER_SIZES } from '../../constants/theme-font-resizer.constants';

export const ThemeFontResizerComponent: any = {
    projectionContent: '',
    inputPropsValues: {
        fontSizes: [...THEME_FONT_RESIZER_SIZES],
        selectedFontSize: ''
    }
}
