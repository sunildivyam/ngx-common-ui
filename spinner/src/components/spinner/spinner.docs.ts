
import { SpinnerMode } from '../../interfaces/spinner.interface';

export const SpinnerComponent: any = {
    projectionContent: '',
    inputPropsValues: {
        show: true,
        mode: SpinnerMode.Spinner,
    }
}
